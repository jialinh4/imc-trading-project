[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "imc-trading-project"
version = "0.1.0"
description = "Scaffold migrated from backtester-refined.ipynb"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
